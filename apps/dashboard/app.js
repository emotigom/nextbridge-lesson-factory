(async()=>{const data=await fetch('/data/status.json',{cache:'no-store'}).then(r=>r.json());const summary=document.querySelector('#summary');summary.innerHTML=`<span class="status">${data.releaseDecision}</span><h2>${data.title}</h2><p><b>${data.stage}</b> · ${data.version}</p><p class="muted">${data.note}</p>`;document.querySelector('#gates').innerHTML=data.gates.map(g=>`<article class="gate"><span class="status">${g.status}</span><h3>${g.name}</h3><p class="muted">${g.detail}</p></article>`).join('')})().catch(e=>{document.querySelector('#summary').textContent='상태 데이터를 읽지 못했습니다: '+e.message});
