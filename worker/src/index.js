export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/api/health')return Response.json({ok:true,service:'nextbridge-lesson-factory'});if(url.pathname.startsWith('/api/releases/')){const courseId=decodeURIComponent(url.pathname.split('/').pop());const res=await env.ASSETS.fetch(new URL('/data/releases.json',url));if(!res.ok)return new Response('release index unavailable',{status:503});const data=await res.json();const release=(data.releases||[]).find(x=>x.courseId===courseId);return release?Response.json(release):new Response('not found',{status:404});}return env.ASSETS.fetch(request)}};
